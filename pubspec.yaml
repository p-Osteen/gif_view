name: gif_view
description: Load GIF images and can set framerate
version: 1.0.3
homepage: https://github.com/RafaelBarbosatec/gif_view

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
